# 2026-08-06 日志

## 项目状态

### AOS（All in One System）项目

**关键决策（来自浪哥）**：
1. 产品名：AOS（All in One System）
2. 许可证：零商业纠纷，所有选型需排查许可证
3. 输入法：Fcitx5 + Rime（雾凇）立即执行
4. 不招人，团队自己解决所有技术问题

**Base OS**：Ubuntu 24.04 LTS（代号 Noble Numbat）；标准支持 2029-04，付费扩展支持 2034-04。

**核心架构**：
- Ubuntu 24.04 LTS base（debootstrap 需网络，企业防火墙封死 apt）
- Cinnamon 6.0.4（官方源自带，无 PPA）
- Wine 9.0（官方源自带）
- Fcitx5 5.1.7 + fcitx5-rime 5.1.4（官方 universe 源）

**环境约束**：
- 企业防火墙：封堵 `archive.ubuntu.com`/`security.ubuntu.com`（HTTP/HTTPS），清华/中科大镜像均无法访问
- GitHub API 能通（200 OK）；Release CDN 小文件能下（Range），大文件（>2MB）CDN 握手超时
- WSL 内 sudo 密码未设置（`wsl -u root` 绕过）
- apt 网络全断，只能靠 GitHub Actions 云端 runner

**钉钉包**：v2.1.22（MIT 许可证，207MB 安装后大小）已通过分段 Range 下载完整落盘
路径：`packages/dingtalk-2.1.22-amd64.deb`

**GitHub Actions CI**：
- `wine-compat.yml`：Wine 兼容性矩阵测试
- `build-iso.yml`：完整 ISO 构建（debootstrap + chroot + squashfs + xorriso）
- 等待：GitHub 账号

**Git 状态**：branch main，8 commits 本地。
- **推送阻塞**：github.com:443 被企业防火墙封死，git push 走不通。
- **解决路径**：主会话用 GitHub REST API 同步 commits（主会话负责 sync）。
- **钉钉 deb**：已从 git 移除，本地文件保留（`packages/` 不进 git）。

## 今日交付文件
- `reports/OS_Wine兼容性摸底_Week1-2.md` ✅
- `reports/OS_Phase1_Week3_桌面定制.md` ✅
- `reports/OS_Phase1_开发计划.md` ✅
- `LICENSE_COMPLIANCE.md` ✅（新增）
- `README.md` ✅（新增）
- `.github/workflows/build-iso.yml` ✅（新增）
- `.github/workflows/wine-compat.yml` ✅（新增）
- `scripts/build-iso.sh` ✅（新增）
- `packages/dingtalk-2.1.22-amd64.deb` ✅（新增）

## 核心原则更新（2026-08-07）
**AOS 必须是独立桌面 OS 品牌，不是 Ubuntu/Cinnamon 换皮**。详见 `docs/AOS_品牌规范.md`。

## 明天待办
1. 确认 GitHub 账号 → 推送代码 → 触发 GitHub Actions ISO 构建
2. 等 ISO artifact 下载回本地
3. Week 4：第一个可启动 ISO 实测
4. AOS Logo + 壁纸设计（Week 3 自研任务）
